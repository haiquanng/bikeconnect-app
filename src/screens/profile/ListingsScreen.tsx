import React from 'react';
import PlaceholderScreen from './PlaceholderScreen';

const ListingsScreen = ({ navigation }: any) => {
  return (
    <PlaceholderScreen navigation={navigation} title="Sản phẩm đang bán" />
  );
};

export default ListingsScreen;

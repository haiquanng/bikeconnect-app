import React from 'react';
import PlaceholderScreen from './PlaceholderScreen';

const OrdersScreen = ({ navigation }: any) => {
  return <PlaceholderScreen navigation={navigation} title="Đơn hàng" />;
};

export default OrdersScreen;
